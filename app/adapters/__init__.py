"""Bundled Source Adapters.

Each module here implements one ``Adapter`` and is wired up by
``app.adapters_bootstrap``. Adapter plugins shipped outside the dispatcher
register themselves the same way; this package is the in-tree home for
adapters we maintain alongside the core.
"""
